# Backend Python dependencies with pinned versions for reproducible builds.

# ---- Web Framework ----------------------------------------------------------
fastapi==0.111.2          # Async web framework; handles routing, validation, and OpenAPI docs
uvicorn[standard]==0.29.0 # ASGI server that runs the FastAPI app; [standard] adds WebSocket support

# ---- Data Validation & Settings ---------------------------------------------
pydantic==2.8.0               # Validates request bodies and response schemas at runtime
pydantic-settings==2.3.4      # Extends pydantic to load settings from .env files and env vars
email-validator==2.2.0        # Required by pydantic's EmailStr field type

# ---- Database ---------------------------------------------------------------
SQLAlchemy==2.0.33            # ORM that generates and executes SQL against Postgres
asyncpg==0.29.0               # Async Postgres driver that SQLAlchemy uses under the hood

# ---- Authentication ---------------------------------------------------------
passlib[bcrypt]==1.7.4        # Hashes passwords with bcrypt; [bcrypt] installs the C extension
python-jose[cryptography]==3.3.0 # Creates and verifies JWT tokens
python-multipart==0.0.9       # Parses multipart/form-data bodies; required for file uploads

# ---- PDF Parsing ------------------------------------------------------------
pypdf==4.4.0                  # Extracts text from PDF files without any system dependencies

# ---- AI / LLM ---------------------------------------------------------------
langchain==0.2.16             # Orchestration layer that manages prompt building and LLM calls
langchain-google-genai==1.0.10 # LangChain adapter for the Gemini API
google-generativeai==0.7.2    # Google's official Python SDK that langchain-google-genai wraps

# ---- Vector Database --------------------------------------------------------
chromadb==0.5.23              # Stores and queries document embeddings for semantic search

# ---- Utilities --------------------------------------------------------------
python-dotenv==1.0.1          # Loads .env files into os.environ; used during local development

# ---- Testing ----------------------------------------------------------------
pytest==8.4.3
httpx==0.27.0                 # Async HTTP client used by FastAPI's TestClient
pytest-asyncio==0.23.8        # Enables async test functions with pytest
