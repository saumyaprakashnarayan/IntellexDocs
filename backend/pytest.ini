[pytest]
# pytest-asyncio >= 0.21 requires an explicit asyncio_mode.
# "auto" makes every async test function and fixture run in the event loop
# automatically, without needing the @pytest.mark.asyncio decorator.
asyncio_mode = auto
